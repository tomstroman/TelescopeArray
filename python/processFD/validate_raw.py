#import subprocess as sp

#def validate_raw(dst, expected):
  #cmd = '{0} -brtime -lrtime {1}'.format(
  #with sp.popen(
  
__package__ = 'TelescopeArray.processFD'
print(__package__)
from ..ta_common import ta
print(ta.sitenames)